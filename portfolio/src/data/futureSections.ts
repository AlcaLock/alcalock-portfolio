import type { FutureSectionKey } from "@/types";

/**
 * Placeholder sections waiting for real content. Remove a key here
 * (and from messages) once it has real items to show, and build a
 * dedicated section the same way Experience/Projects are built.
 */
export const futureSections: FutureSectionKey[] = [
  "certifications",
  "openSource",
  "hackathons",
  "awards",
  "publications",
  "articles",
];
